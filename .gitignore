# keep-sorted start newline_separated=yes
# Hugo build lock file
.hugo_build.lock

# pre-commit configuration file
.pre-commit-config.yaml

# Hugo build output (rendered static site)
public/

# Hugo resource cache (processed images, compiled SCSS, etc.)
resources/_gen/
# keep-sorted end
